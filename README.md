## HMCTS DEVELOPER TEST FRONTEND

Runs on Localhost:3100 by default

Run with https://github.com/Cheechiem/hmcts-application-task-backend for full functionality

To begin with, you should be able to run this by running:
1) `yarn install`
2) `yarn webpack`
3) `yarn start:dev` or navigate to package.json and run the script manually

